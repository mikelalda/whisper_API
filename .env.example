# Model to use (HuggingFace model ID)
MODEL_ID=xezpeleta/whisper-large-v3-eu

# Device: "cuda" or "cpu" (auto-detected if not set)
# DEVICE=cuda

# Max upload file size in MB
MAX_FILE_SIZE_MB=100

# Batch size for long-form transcription
BATCH_SIZE=16

# Server
HOST=0.0.0.0
PORT=8000
